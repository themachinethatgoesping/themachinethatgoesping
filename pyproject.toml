[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "themachinethatgoesping"
authors = [{name = "Peter Urban", email = "peter.urban@ugent.be"},{name = "More to follow"}]
dynamic = ["version", "description"]
